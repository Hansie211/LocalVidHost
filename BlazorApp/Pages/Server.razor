@page "/server"
@layout TheatherLayout
@using BlazorApp.Connection.Server;
@inherits ServerPage;

<div id="video-box">
    <video id="video" controls="controls">
        <source id="video-source" />
    </video>
</div>

@code {
    // private string sourceUrl = "https://sample-videos.com/video123/mp4/240/big_buck_bunny_240p_30mb.mp4";

    // load

    protected override async Task OnInitializedAsync()
    {
        await InitAsync();
    }
}
